# 阿里云快速部署指南

## 🎯 最简单的方式

由于 SSH 连接可能需要密钥认证，我为你创建了一个**自动化部署脚本**，你只需要在服务器上执行即可！

---

## 📋 部署步骤

### 第一步：连接到服务器

**在阿里云控制台**：
1. 进入"轻量应用服务器"
2. 找到你的服务器 `Docker-odnf`
3. 点击"远程连接"
4. 选择"Workbench远程连接"

### 第二步：执行自动部署脚本

在服务器终端中，执行以下命令：

```bash
# 下载并执行自动部署脚本
curl -fsSL https://raw.githubusercontent.com/Bupoo123/AIO2/master/auto-deploy.sh -o /tmp/auto-deploy.sh
chmod +x /tmp/auto-deploy.sh
sudo /tmp/auto-deploy.sh
```

**或者，如果 GitHub 访问有问题，可以手动创建**：

```bash
# 创建脚本文件
cat > /tmp/auto-deploy.sh << 'EOF'
# 将 auto-deploy.sh 的内容粘贴到这里
EOF

# 执行脚本
chmod +x /tmp/auto-deploy.sh
sudo /tmp/auto-deploy.sh
```

### 第三步：配置环境变量

脚本会提示你编辑 `.env` 文件，执行：

```bash
nano /opt/AIO2/.env
```

配置内容：
```env
MONGODB_URI=mongodb+srv://AIO2admin:31493170@cluster0.gpq75zd.mongodb.net/jeyi-toolhub?retryWrites=true&w=majority
JWT_SECRET=484a294c2d4f51bfa0767c012935105b4270d0247331e8168e2512f64638bac1
PORT=3000
NODE_ENV=production
FRONTEND_URL=http://120.26.172.158
```

保存：按 `Ctrl+X`，然后 `Y`，再 `Enter`

### 第四步：配置防火墙

**在阿里云控制台**：
1. 进入服务器详情页
2. 点击"防火墙"标签
3. 添加规则：
   - 端口：`80`
   - 协议：`TCP`
   - 策略：`允许`

### 第五步：创建管理员账号

```bash
cd /opt/AIO2
docker-compose exec backend sh -c "cd /app/backend && node scripts/createAdmin.js"
```

---

## ✅ 完成！

访问：`http://120.26.172.158`

管理员账号：
- 用户名：`admin`
- 密码：`123456`

---

## 🔧 如果遇到问题

### 查看日志
```bash
cd /opt/AIO2
docker-compose logs -f
```

### 检查服务状态
```bash
cd /opt/AIO2
docker-compose ps
```

### 重启服务
```bash
cd /opt/AIO2
docker-compose restart
```

---

## 📞 需要帮助？

如果脚本执行过程中遇到问题，把错误信息发给我，我会帮你解决！

