# Vercel 项目配置说明

## 📋 根据你的项目结构，正确的配置应该是：

### 1. **Vercel Team**
- ✅ 保持：`bupoo's projects` (Hobby)
- 不需要修改

### 2. **Project Name**
- ✅ 保持：`aio-2`
- 不需要修改

### 3. **Framework Preset**
- ✅ 保持：`Other`
- 这是正确的，因为我们使用的是 Express + 静态文件，不是标准框架

### 4. **Root Directory**
- ✅ 保持：`./`
- 这是正确的，项目根目录就是仓库根目录

### 5. **Build and Output Settings**（重要！）

#### **Build Command（构建命令）**
```
cd backend && npm install
```
或者留空（因为前端是静态文件，不需要构建）

**说明**：
- 前端文件（`frontend/`）是静态 HTML/CSS/JS，不需要构建
- 后端依赖在 `backend/` 目录，需要安装
- Serverless 函数（`api/`）会自动处理

#### **Output Directory（输出目录）**
```
frontend
```
**说明**：
- 静态文件在 `frontend/` 目录
- Vercel 需要知道从哪里提供静态文件

#### **Install Command（安装命令）**
保持默认：
```
yarn install, pnpm install, npm install, or bun install
```
或者可以指定：
```
npm install && cd backend && npm install
```

## 🎯 推荐配置总结

| 配置项 | 推荐值 | 说明 |
|--------|--------|------|
| Framework Preset | `Other` | ✅ 已正确 |
| Root Directory | `./` | ✅ 已正确 |
| Build Command | `cd backend && npm install` | 安装后端依赖 |
| Output Directory | `frontend` | 静态文件目录 |
| Install Command | 默认或 `npm install && cd backend && npm install` | 安装依赖 |

## ⚠️ 注意事项

1. **Build Command 可以留空**：如果前端不需要构建，可以留空，Vercel 会自动处理
2. **Output Directory 很重要**：必须指向 `frontend`，这样 Vercel 才知道静态文件在哪里
3. **API 路由会自动处理**：`api/[...].js` 会被 Vercel 自动识别为 serverless 函数

## 🔍 如果配置后仍然 404

可能需要检查：
1. `vercel.json` 的路由配置是否正确
2. 环境变量是否已设置（MONGODB_URI, JWT_SECRET, FRONTEND_URL）
3. 部署日志中是否有错误信息

