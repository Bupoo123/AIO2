# 👤 在 MongoDB Atlas 中创建管理员账号

## 🎯 方法一：使用脚本（最简单）

### 步骤

1. **打开终端，运行脚本**
   ```bash
   cd /Users/bupoo/Github/AIO2/backend
   node scripts/createAdmin.js
   ```

2. **脚本会自动**：
   - 连接到 MongoDB Atlas
   - 创建管理员账号（用户名：`admin`，密码：`123456`）
   - 或更新现有账号为管理员

3. **完成**
   - 看到 "✅ 管理员账号创建成功！" 表示完成

### 如果连接失败

如果脚本连接失败，可以手动设置环境变量：

```bash
export MONGODB_URI="mongodb+srv://AIO2admin:31493170@cluster0.gpq75zd.mongodb.net/jeyi-toolhub?retryWrites=true&w=majority"
node scripts/createAdmin.js
```

---

## 🎯 方法二：使用 MongoDB Compass（图形界面）

### 步骤

1. **下载 MongoDB Compass**
   - 访问：https://www.mongodb.com/products/compass
   - 下载并安装

2. **连接数据库**
   - 打开 Compass
   - 连接字符串：
     ```
     mongodb+srv://AIO2admin:31493170@cluster0.gpq75zd.mongodb.net/jeyi-toolhub?retryWrites=true&w=majority
     ```
   - 点击 "Connect"

3. **创建管理员用户**
   - 连接到数据库后，找到 `users` 集合
   - 如果集合不存在，会自动创建
   - 点击 "Add Data" → "Insert Document"
   - 插入以下文档：
     ```json
     {
       "username": "admin",
       "email": "admin@jeyi.com",
       "password_hash": "$2a$10$5tTJ2.ndOUx2XvQ5f9Hu5uZ.gUJLi6xMRf/R7XjhyYXOW8KUiteIC",
       "role": "admin"
     }
     ```
   - **注意**：上面的 password_hash 是 `123456` 的加密值

4. **或修改现有用户**
   - 如果已有用户，找到该用户文档
   - 点击 "Edit"
   - 将 `role` 字段改为 `"admin"`
   - 保存

---

## 🎯 方法三：使用 MongoDB Shell

### 步骤

1. **安装 MongoDB Shell**
   ```bash
   # macOS
   brew install mongosh
   
   # 或下载：https://www.mongodb.com/try/download/shell
   ```

2. **连接数据库**
   ```bash
   mongosh "mongodb+srv://AIO2admin:31493170@cluster0.gpq75zd.mongodb.net/jeyi-toolhub"
   ```

3. **创建管理员（需要先加密密码）**
   
   由于密码需要加密，建议使用方法一（脚本）或方法二（Compass）。

---

## ✅ 验证管理员账号

创建完成后，访问你的网站：

1. 打开：`https://your-domain.vercel.app`
2. 使用管理员账号登录：
   - 用户名：`admin`
   - 密码：`123456`
3. 登录成功后，应该能看到右上角的 **"管理后台"** 按钮

---

## 🔧 如果登录失败

1. **检查账号是否创建成功**
   - 使用 MongoDB Compass 查看 `users` 集合
   - 确认有 `username: "admin"` 且 `role: "admin"` 的用户

2. **检查密码是否正确**
   - 如果密码不是 `123456`，需要重新运行脚本或手动创建

3. **重新运行脚本**
   ```bash
   cd /Users/bupoo/Github/AIO2/backend
   node scripts/createAdmin.js
   ```

---

**推荐使用方法一（脚本），最简单快捷！** 🚀

